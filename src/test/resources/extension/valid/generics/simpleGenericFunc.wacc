begin

newtype List<A> is union
    Nil;
    Node of (A elem, List<A> next);
end

List<K> fromTwo(K a1, K a2) where K: Ord is
    List<K> e = Nil();
    var l1 = Node(a1, e);
    var l2 = Node(a2, l1);
    return l2;
end

var a = fromTwo(1, 2);
List<int> e = Nil();

end